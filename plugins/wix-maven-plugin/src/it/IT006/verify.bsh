import java.io.*;

try
{
    File file = new File( basedir, "WixMSI/target/IT006.msi" );
    if ( !file.isFile() )
    {
        System.err.println( "Could not find installation package: " + file );
        return false;
    }
}
catch( Throwable t )
{
    t.printStackTrace();
    return false;
}

return true;
